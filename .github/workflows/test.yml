name: Testing
on: [workflow_dispatch]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
      - name: Run tests
        run: |
          npm i
          npm test